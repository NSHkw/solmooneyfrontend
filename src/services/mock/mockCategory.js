// src/services/mock/mockCategory.js

const addCategory = (name, color) => {};

const delCategory = () => {};

const CATEGORY_API = { addCategory, delCategory };
//카테고리 추가, 삭제 api 정도만 있어도 되는거 같음

export default CATEGORY_API;
